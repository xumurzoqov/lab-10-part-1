#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    int *p1 = &a, *p2 = &b, *p3 = &c;


    int max_num = *p1;
    if (*p2 > max_num) max_num = *p2;
    if (*p3 > max_num) max_num = *p3;

    cout << max_num;
    return 0;
}
